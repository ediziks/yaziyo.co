### disabled for now, try it later
#files:
#  "/etc/httpd/conf.d/ssl_rewrite.conf":
#    mode: "000644"
#    owner: root
#    group: root
#    content: |
#      RewriteEngine On
#      &lt;If "-n '%{HTTP:X-Forwarded-Proto}' &amp;&amp; %{HTTP:X-Forwarded-Proto} != 'https'"&gt;
#      RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI} [R,L]
#      &lt;/If&gt;